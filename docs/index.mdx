---
name: Formin
route: /
---

import { Playground } from 'docz'
import { Formin } from '../src'

# Formin

## Basic usage

<Playground>
  <Formin
    onSubmit={({ values }) => {
      console.log(values)
    }}
    onChange={values => {
      console.log(values)
    }}
  >
    {({ getFormProps, getInputProps }) => (
      <form {...getFormProps()}>
        <input {...getInputProps({ name: 'text1', type: 'text' })} />
        <input
          {...getInputProps({ name: 'text2', type: 'text', required: true })}
        />
        <button>Submit</button>
      </form>
    )}
  </Formin>
</Playground>
