sudo: false
language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
notifications:
  email: false
node_js: '8'
script:
  - kcd-scripts validate lint,build,test:cover
after_success: kcd-scripts travis-after-success
branches:
  only: master
